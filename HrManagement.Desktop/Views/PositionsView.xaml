<UserControl x:Class="HrManagement.Desktop.Views.PositionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="PositionsView_Loaded">

    <Grid Margin="24">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel>
            <TextBlock Text="Chức danh"
                       FontSize="26"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource PrimaryTextBrush}" />

            <TextBlock Margin="0,6,0,0"
                       Text="Quản lý danh mục chức danh được sử dụng trong hệ thống."
                       Foreground="{DynamicResource SecondaryTextBrush}" />
        </StackPanel>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,20,0,16">

            <Button Content="Thêm chức danh"
                    Command="{Binding AddPositionCommand}" Height="30" Width="100" />

            <Button Content="Sửa"
                    Command="{Binding EditPositionCommand}"
                    Margin="8,0,0,0" Width="40" />

            <Button Content="Ngừng sử dụng"
                    Command="{Binding DeactivatePositionCommand}"
                    Margin="8,0,0,0" Width="100" />

            <Button Content="Kích hoạt lại"
                    Command="{Binding ReactivatePositionCommand}"
                    Margin="8,0,0,0" Width="90" />

            <Button Content="Xem nhân viên"
                    Command="{Binding ViewEmployeesCommand}"
                    Margin="8,0,0,0"
                    Padding="12,6"
                    ToolTip="Xem các nhân viên có chức danh được chọn" />

        </StackPanel>

        <!-- Content -->
        <Grid Grid.Row="2">

            <DataGrid ItemsSource="{Binding PositionRows}"
                      SelectedItem="{Binding SelectedPositionRow,
                                             Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      SelectionMode="Single"
                      SelectionUnit="FullRow">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="Mã"
                                        Width="140"
                                        Binding="{Binding Code}" />

                    <DataGridTextColumn Header="Tên chức danh"
                                        Width="*"
                                        Binding="{Binding Name}" />

                    <DataGridTemplateColumn Header="Nhân sự"
                        Width="190">

                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>

                                <TextBlock Text="{Binding StaffingSummaryText}"
                                           ToolTip="{Binding StaffingDetailText}"
                                           VerticalAlignment="Center" />

                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>

                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Trạng thái"
                                            Width="160">

                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>

                                <TextBlock>
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">

                                            <Setter Property="Text"
                                                    Value="Ngừng sử dụng" />

                                            <Setter Property="Foreground"
                                                    Value="{DynamicResource SecondaryTextBrush}" />

                                            <Style.Triggers>

                                                <DataTrigger Binding="{Binding IsActive}"
                                                             Value="True">

                                                    <Setter Property="Text"
                                                            Value="Đang sử dụng" />

                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource PrimaryTextBrush}" />

                                                </DataTrigger>

                                            </Style.Triggers>

                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>

                    </DataGridTemplateColumn>

                </DataGrid.Columns>

            </DataGrid>

            <TextBlock Text="Đang tải danh sách chức danh..."
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center">

                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility"
                                Value="Collapsed" />

                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}"
                                         Value="True">
                                <Setter Property="Visibility"
                                        Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>

            </TextBlock>

        </Grid>

        <TextBlock Grid.Row="2"
                   Margin="0,8,0,0"
                   VerticalAlignment="Bottom"
                   Text="{Binding ErrorMessage}"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource WarningBrush}" />

    </Grid>

</UserControl>
