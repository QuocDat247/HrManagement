<Window x:Class="HrManagement.Desktop.Views.PositionEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="460"
        Height="360"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource DialogWindowStyle}">

    <Grid Margin="24">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>

            <TextBlock Text="{Binding Title}"
                       Style="{StaticResource DialogTitleTextStyle}" />

            <TextBlock Margin="0,7,0,0"
                       Text="Thông tin chức danh được dùng làm dữ liệu danh mục trong hệ thống."
                       Style="{StaticResource DialogSubtitleTextStyle}" />

        </StackPanel>

        <StackPanel Grid.Row="1"
                    Margin="0,24,0,0">

            <TextBlock Text="Mã chức danh"
                       Style="{StaticResource DialogLabelTextStyle}" />

            <TextBox Margin="0,6,0,16"
                     Text="{Binding Code,
                                    Mode=TwoWay,
                                    UpdateSourceTrigger=PropertyChanged}"
                     MaxLength="32" />

            <TextBlock Text="Tên chức danh"
                       Style="{StaticResource DialogLabelTextStyle}" />

            <TextBox Margin="0,6,0,0"
                     Text="{Binding Name,
                                    Mode=TwoWay,
                                    UpdateSourceTrigger=PropertyChanged}"
                     MaxLength="200" />

            <TextBlock Margin="0,14,0,0"
                       Text="{Binding ErrorMessage}"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource WarningBrush}" />

        </StackPanel>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">

            <Button Width="90"
                    Margin="0,0,10,0"
                    Content="Hủy"
                    IsCancel="True" Height="36"
                    Style="{StaticResource DialogSecondaryButtonStyle}" />

            <Button Width="100"
                    Content="Lưu"
                    Command="{Binding ConfirmCommand}"
                    Style="{StaticResource DialogPrimaryButtonStyle}" />

        </StackPanel>

    </Grid>

</Window>
