<UserControl x:Class="HrManagement.Desktop.Views.DepartmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="DepartmentsView_Loaded">

    <Grid Margin="24">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel>
            <TextBlock Text="Phòng ban"
                       FontSize="26"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource PrimaryTextBrush}" />

            <TextBlock Margin="0,6,0,0"
                       Text="Quản lý danh mục phòng ban được sử dụng trong hệ thống."
                       Foreground="{DynamicResource SecondaryTextBrush}" />
        </StackPanel>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,20,0,16">

            <Button Content="Thêm phòng ban"
                    Command="{Binding AddDepartmentCommand}" Height="27" Width="100" />

            <Button Content="Sửa"
                    Command="{Binding EditDepartmentCommand}"
                    Margin="8,0,0,0" Width="40" />

            <Button Content="Ngừng sử dụng"
                    Command="{Binding DeactivateDepartmentCommand}"
                    Margin="8,0,0,0" Width="100" />

            <Button Content="Kích hoạt lại"
                    Command="{Binding ReactivateDepartmentCommand}"
                    Margin="8,0,0,0" Width="89" />

            <Button Content="Xem nhân viên"
                    Command="{Binding ViewEmployeesCommand}"
                    Margin="8,0,0,0"
                    Padding="12,6"
                    ToolTip="Xem các nhân viên thuộc phòng ban được chọn" />

        </StackPanel>

        <!-- Content -->
        <Grid Grid.Row="2">

            <DataGrid ItemsSource="{Binding Departments}"
                      SelectedItem="{Binding SelectedDepartment,
                                             Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      SelectionMode="Single"
                      SelectionUnit="FullRow">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="Mã"
                                        Width="140"
                                        Binding="{Binding Code}" />

                    <DataGridTextColumn Header="Tên phòng ban"
                                        Width="*"
                                        Binding="{Binding Name}" />

                    <DataGridTemplateColumn Header="Trạng thái"
                                            Width="160">

                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>

                                <TextBlock>
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">

                                            <Setter Property="Text"
                                                    Value="Ngừng sử dụng" />

                                            <Setter Property="Foreground"
                                                    Value="{DynamicResource SecondaryTextBrush}" />

                                            <Style.Triggers>

                                                <DataTrigger Binding="{Binding IsActive}"
                                                             Value="True">

                                                    <Setter Property="Text"
                                                            Value="Đang sử dụng" />

                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource PrimaryTextBrush}" />

                                                </DataTrigger>

                                            </Style.Triggers>

                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>

                    </DataGridTemplateColumn>

                </DataGrid.Columns>

            </DataGrid>

            <!-- Loading -->
            <TextBlock Text="Đang tải danh sách phòng ban..."
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center">

                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility"
                                Value="Collapsed" />

                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}"
                                         Value="True">
                                <Setter Property="Visibility"
                                        Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>

            </TextBlock>

        </Grid>

        <!-- Error -->
        <TextBlock Grid.Row="2"
                   Margin="0,8,0,0"
                   VerticalAlignment="Bottom"
                   Text="{Binding ErrorMessage}"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource WarningBrush}" />

    </Grid>

</UserControl>
